---
import { Image } from 'astro:assets';
import HeroImg from '../asset/illustration-intro.png';
---

<section class="relative font-secondary">
  <div class="hero-bg"></div>

  <div class="max-auto container max-w-[600px] pt-10">
    <Image src={HeroImg} alt="Hero Img" />

    <div class="text-center">
      <h1 class="mb-4 text-3xl font-700 md:text-4xl">
        All your files in one secure location, accessible anywhere.
      </h1>
      <p class="mb-4">
        Fylo stores all your most important files in one secure location. Access
        them wherever you need, share and collaborate with friends family, and
        co-workers.
      </p>
      <button
        class="relative rounded-full bg-gradient-to-tr from-accent-100 to-accent-200 px-12 py-3 font-700 text-white after:absolute after:left-0 after:top-0 after:h-full after:w-full after:rounded-full after:bg-white after:bg-opacity-0 after:transition-all after:duration-300 after:hover:bg-opacity-40"
      >
        Get Started
      </button>
    </div>
  </div>
</section>

<style>
  .hero-bg {
    background: hsl(217, 28%, 15%) url(../asset/bg-curvy-mobile.svg);
    background-repeat: no-repeat;
    background-position: bottom center;
    background-size: contain;
    width: 100%;
    height: 100%;
    position: absolute;
    bottom: 14rem;
    z-index: -1;
  }

  @media (min-width: 768px) {
    .hero-bg {
      background: hsl(217, 28%, 15%) url(../asset/bg-curvy-desktop.svg);
      background-repeat: no-repeat;
      background-position: bottom center;
      background-size: 100% 279px;
      width: 100%;
      height: 100%;
      position: absolute;
      bottom: 0;
    }
  }
</style>
